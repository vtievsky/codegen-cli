{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Codegen Client",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/codegen-cli/main.go",
            "env": {
                // "CODEGEN_CLI_CODEGEN_URL": "http://127.0.0.1:8080",
                "CODEGEN_CLI_CODEGEN_URL": "http://127.0.0.1:9810",
                "CODEGEN_CLI_DEBUG": "true"
            },
            "args": [
                "abc"
                // // "gen-http-client",
                // // "--packagepath",
                // // "../../",
                // "upload-http-server",
                // "--source",
                // "${workspaceRoot}/tmp/auth-id.yaml",
                // "--service",
                // "auth-id"
            ]
        }
    ]
}